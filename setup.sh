#!/bin/bash
# Setup script that creates all the symbolic links to this repo
# EXECUTE IN THE ROOT OF THIS REPO

ln -s $PWD/bashrc ~/.bashrc
ln -s $PWD/vimrc ~/.vimrc
ln -s $PWD/tmux.conf ~/.tmux.conf
